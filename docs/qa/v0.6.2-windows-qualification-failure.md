# ClipGauge v0.6.2 Windows Qualification Failure

Date: 2026-09-23

## Run evidence

- Workflow run: `35830762489`
- Job: `107082560934`
- Head: `4f109265a4aba453962ea75383bb78b0c90450e8`
- Artifact: `clipgauge-windows-setup`
- Artifact ID: `10737272845`
- Failed step: `Install silently and run deterministic packaged Windows UI qualification`

All earlier build and packaging steps passed. The artifact was uploaded after this failure.

## Exact failed assertion

The qualification snapshot still used the old Create contract:

```js
await text(page, 'Choose AI', 'Create Step 2')
await text(page, 'Choose caption style', 'Create Step 3')
```

`text()` resolves exact text through:

```js
page.getByText(value, { exact: true }).first()
```

The first stale assertion was therefore the exact `Choose AI` locator. The following stale assertion was `Choose caption style`.

- Expected by harness: `Choose AI`, then `Choose caption style`.
- Actual product hierarchy: `What should ClipGauge find?`, then `Choose the balance`.
- Screenshot filename: `setup-1920x1200.png`; no Create screenshot existed.
- Actual UI state: Setup completed, then Create semantic checks began.

## Artifact state

The artifact contained:

- `windows-screenshots/setup-1920x1200.png`
- `windows-screenshots/display-create-1920x1200.json`
- setup, provider, and local-AI screenshots
- the packaged `ClipGauge_0.6.2_x64-setup.exe`
- qualification script snapshots

The artifact did not contain:

- `windows-screenshots/create-1920x1200.png`
- `windows-screenshots/create-hostile-1920x1200.png`

The Create display facts were written before semantic assertions. Create screenshots were written after those assertions. This localizes the failure to the Create semantic contract.

## Product state observed

The packaged Setup screenshot showed the v0.6.2 tabs, Overview content, setup readiness summary, disk state, GPU state, and local-AI choice. It did not show the old three-step Create labels.

The current v0.6.2 Create implementation exposes:

- Source and video-link input;
- Content intent choices;
- Quick scoring choices;
- Collapsed Advanced creation settings;
- Output preference choices inside Advanced;
- Create clips action.

The existing `setup-1920x1200.png` screenshot is the relevant
uploaded visual evidence. It shows the redesigned Setup UI.

## Classification

This is a qualification-harness regression. The failing assertion targeted removed v0.6.1 Create copy. The artifact proves the assertion ran before Create screenshots. No product failure is established by this run.

The repair updates the harness to test the v0.6.2 semantic contract. It does not weaken layout, accessibility, or action assertions.

## Follow-up exact-head failure

The repaired head `cb2473b7489c28e5fa7f81f7a508fbf0d3b67047` produced a second
Windows failure in run `35847813904`, job `107138102145`.

The exact failed assertion was:

```text
locator.waitFor: Timeout 120000ms exceeded.
waiting for getByRole('button', { name: 'Setup & Storage', exact: true }).first()
```

The timeout occurred before semantic Setup checks. The full qualification
script selected a new isolated `CLIPGAUGE_HOME` under the output directory,
after the workflow bootstrapped setup assets and the `onboarded` marker in the
clean runner home. The packaged app remained in onboarding and never rendered
application navigation.

Fresh artifact evidence:

- Artifact: `clipgauge-windows-setup`, ID `10744579444`
- Screenshot: `setup-fresh-1366x768-fresh.png`
- Screenshot state: `Core setup needed`
- Display evidence: `display-setup-fresh-1366x768-fresh.json`
- Application navigation: absent during failed full run

This is a qualification-harness isolation/bootstrap failure. Product failure
is not established. The repair passes the already-clean, bootstrapped runner
home explicitly as the qualification home. The home remains isolated because
the workflow deletes it before setup and seeds only qualification fixtures.
