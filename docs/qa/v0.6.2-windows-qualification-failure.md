# ClipGauge v0.6.2 Windows Qualification Failure

Date: 2026-09-23

## Run evidence

- Workflow run: `35830762489`
- Job: `107082560934`
- Head: `4f109265a4aba453962ea75383bb78b0c90450e8`
- Artifact: `clipgauge-windows-setup`
- Artifact ID: `10737272845`
- Failed step: `Install silently and run deterministic packaged Windows UI qualification`

All earlier build and packaging steps passed. The artifact was uploaded after this failure.

## Exact failed assertion

The qualification snapshot still used the old Create contract:

```js
await text(page, 'Choose AI', 'Create Step 2')
await text(page, 'Choose caption style', 'Create Step 3')
```

`text()` resolves exact text through:

```js
page.getByText(value, { exact: true }).first()
```

The first stale assertion was therefore the exact `Choose AI` locator. The following stale assertion was `Choose caption style`.

- Expected by harness: `Choose AI`, then `Choose caption style`.
- Actual product hierarchy: `What should ClipGauge find?`, then `Choose the balance`.
- Screenshot filename: `setup-1920x1200.png`; no Create screenshot existed.
- Actual UI state: Setup completed, then Create semantic checks began.

## Artifact state

The artifact contained:

- `windows-screenshots/setup-1920x1200.png`
- `windows-screenshots/display-create-1920x1200.json`
- setup, provider, and local-AI screenshots
- the packaged `ClipGauge_0.6.2_x64-setup.exe`
- qualification script snapshots

The artifact did not contain:

- `windows-screenshots/create-1920x1200.png`
- `windows-screenshots/create-hostile-1920x1200.png`

The Create display facts were written before semantic assertions. Create screenshots were written after those assertions. This localizes the failure to the Create semantic contract.

## Product state observed

The packaged Setup screenshot showed the v0.6.2 tabs, Overview content, setup readiness summary, disk state, GPU state, and local-AI choice. It did not show the old three-step Create labels.

The current v0.6.2 Create implementation exposes:

- Source and video-link input;
- Content intent choices;
- Quick scoring choices;
- Collapsed Advanced creation settings;
- Output preference choices inside Advanced;
- Create clips action.

The existing `setup-1920x1200.png` screenshot is the relevant
uploaded visual evidence. It shows the redesigned Setup UI.

## Classification

This is a qualification-harness regression. The failing assertion targeted removed v0.6.1 Create copy. The artifact proves the assertion ran before Create screenshots. No product failure is established by this run.

The repair updates the harness to test the v0.6.2 semantic contract. It does not weaken layout, accessibility, or action assertions.

## Follow-up exact-head failure

The repaired head `cb2473b7489c28e5fa7f81f7a508fbf0d3b67047` produced a second
Windows failure in run `35847813904`, job `107138102145`.

The exact failed assertion was:

```text
locator.waitFor: Timeout 120000ms exceeded.
waiting for getByRole('button', { name: 'Setup & Storage', exact: true }).first()
```

The timeout occurred before semantic Setup checks. The full qualification
script selected a new isolated `CLIPGAUGE_HOME` under the output directory,
after the workflow bootstrapped setup assets and the `onboarded` marker in the
clean runner home. The packaged app remained in onboarding and never rendered
application navigation.

Fresh artifact evidence:

- Artifact: `clipgauge-windows-setup`, ID `10744579444`
- Screenshot: `setup-fresh-1366x768-fresh.png`
- Screenshot state: `Core setup needed`
- Display evidence: `display-setup-fresh-1366x768-fresh.json`
- Application navigation: absent during failed full run

This is a qualification-harness isolation/bootstrap failure. Product failure
is not established. The repair passes the already-clean, bootstrapped runner
home explicitly as the qualification home. The home remains isolated because
the workflow deletes it before setup and seeds only qualification fixtures.

## Follow-up exact-head failure: descriptive radio names

The next repaired head `e612342249aa6dac843ed1f44f3c6341605d159a` produced a
third Windows failure in run `35852523884`, job `107153299840`.

The exact failed assertion was:

```text
locator.waitFor: Timeout 120000ms exceeded.
waiting for getByRole('radio', { name: 'Private / Local', exact: true })
```

The failure occurred in `createState` at the scoring-mode check. Every setup,
provider, local-model, credential-removal, and earlier Create viewport state
passed. The packaged build and installer also passed.

Artifact evidence:

- Artifact: `clipgauge-windows-setup`
- The artifact contains `display-create-1920x1200.json`.
- The artifact has no `create-1920x1200.png`, because the assertion preceded capture.
- The preceding state screenshots passed through `1920x1200` setup checks.

The product source defines scoring choices as native `role="radio"` buttons.
Each button contains a visible label and descriptive text. Therefore its
accessible name includes both strings. Exact accessible-name matching for
`Private / Local` was brittle. The visible product label remained present.

Expected UI state: the scoring choices show `Private / Local`, `Hybrid`, and
`Best Quality` within the scoring group.

Actual observed state: the packaged qualification timed out locating the exact
accessible name `Private / Local`.

Classification: qualification-harness selector regression. Product failure is
not established. The repair matches the semantic radio role and visible label,
while preserving the required scoring-choice assertion.

## Follow-up exact-head failure: unsupported hostile fixtures

The next head `74c8182923817b48c3c0108467c00013a8b11150` failed in run
`35857389547`, job `107169031647`.

The exact failed assertion was:

```text
locator.waitFor: Timeout 120000ms exceeded.
waiting for locator('.sidebar-session strong').filter({ hasText: 'MrBeast 2' }).first() to be visible
```

The failure occurred during `create-hostile 1920x1200`. The artifact contains
`create-1920x1200.png`, `create-1920x1200.json`, and
`display-create-hostile-1920x1200.json`. It contains no hostile screenshot,
because the assertion preceded capture.

The captured normal Create screenshot shows the actual state: the sidebar says
`Sessions unavailable. Open Sessions and retry.` The native session listing
failed frontend validation because the hostile fixtures contained only
`ingest.json`. Their native payload therefore exposed `source_type: null`,
which is outside the supported `JobSummary` metadata contract. This was not a
product rendering failure and was not a valid session fixture.

Expected UI state: the sidebar shows the two hostile session titles, while the
normal Create contract remains visible.

Actual UI state: the normal Create UI rendered, but session loading reported
`Sessions unavailable`; the first hostile title was absent.

Classification: qualification-harness fixture regression. Product failure is
not established. The repair seeds `input.json` and `lifecycle.json` with the
same supported metadata contract used by the mixed Sessions fixtures. The
fixture regression test now requires those fields.

## Follow-up exact-head failure: no-recommendation presentation contract

The next head `6d27af3dae0ded37fed5159d0067ea0cdbf0927c` failed in run
`35861975944`, job `107184134021`.

The exact failed assertion was:

```text
Error: Sessions fixture mismatch for Controlled no-clip fixture: {"title":"Controlled no-clip fixture","source":"controlled-no-clip.mp4","state":"Analysis complete · no recommended clips","detail":"The quality bar was not met.","action":"Open analysis"}
```

The failure occurred during `sessions 1920x1200`, after all prior packaged
states passed, including `create-hostile 1920x1200`. The artifact contains
`display-sessions-1920x1200.json` and the Sessions screenshot.

Expected UI state: the no-recommendation row shows the v0.6.2 terminal state,
an explanatory quality-bar detail, and `Open analysis`.

Actual UI state: the state text was `Analysis complete · no recommended
clips`; the detail was `The quality bar was not met.`; the action was
`Open analysis`.

Classification: qualification-harness contract regression. Product failure is
not established. The harness expected `no recommended clips` in the detail
field, although the redesigned v0.6.2 UI places it in the state field and
uses the quality-bar explanation as detail. The repair asserts that current
semantic presentation directly and adds a regression test.

## Follow-up exact-head failure: viewport scroll leakage

The repaired head `3c6b7f42f6084b320c0a152b88ac74ea83406058` failed in run
`35867006588`, job `107201058894`.

The exact failed assertion was:

```text
Error: layout invariant failed for setup: {"css_viewport":{"width":1280,"height":720},"sidebar":{"left":0,"right":250,"top":-2914,"bottom":720.515625,"width":250,"height":3634.515625},"main":{"left":250,"right":1265,"top":-2914,"bottom":720.515625,"width":1015,"height":3634.515625},"max_sidebar_descendant_right":250,"sidebar_contained":true,"main_begins_after_sidebar":true,"no_horizontal_scroll":true,"clipped_controls":1,"overlapping_cards":[],"active_breakpoint":"wide"}
```

The failure occurred at the first additional viewport, `setup 1280x720`,
after all earlier packaged states passed. The artifact contains
`setup-1280x720.png` and `display-setup-1280x720.json`.

Expected UI state: each viewport begins at the top of its selected page,
then measures layout and captures evidence.

Actual UI state: the layout roots had `top: -2914`, proving that prior page
scroll had leaked into the new viewport. The invariant then reported one
clipped control. The screenshot showed the Setup Overview itself, with no
horizontal overflow or overlapping cards.

Classification: qualification-harness state-isolation regression. Product
failure is not established. The repair resets window, document, and app-shell
scroll positions after every logical viewport is confirmed, then retains the
existing strict clipping and overflow assertions. A regression test covers
the reset contract.

## Follow-up exact-head failure: navigation re-scrolled the tall sidebar

The next head `257587867eea5af8eb5334aa65e86a16901fea01` failed in run
`35874459155`, job `107226657963`.

The exact failed assertion was the same layout invariant:

```text
Error: layout invariant failed for setup: {"css_viewport":{"width":1280,"height":720},"sidebar":{"left":0,"right":250,"top":-2914,"bottom":720.515625,"width":250,"height":3634.515625},"main":{"left":250,"right":1265,"top":-2914,"bottom":720.515625,"width":1015,"height":3634.515625},"max_sidebar_descendant_right":250,"sidebar_contained":true,"main_begins_after_sidebar":true,"no_horizontal_scroll":true,"clipped_controls":1,"overlapping_cards":[],"active_breakpoint":"wide"}
```

The prior reset ran before navigation. Playwright then scrolled the tall
sidebar while locating the navigation button, restoring the leaked offset.
The same exact `setup 1280x720` state failed after all earlier states passed.

Classification: qualification-harness state-isolation regression. Product
failure is not established. The repair now resets window, document, and
app-shell scroll positions immediately after every navigation click as well
as after viewport setup. Strict clipping and overflow checks remain enabled.

The follow-up run showed that this reset still occurred before the final
route layout settled. The finding remains open for fresh qualification.

## Follow-up exact-head failure: invariant-level reset still pending

The next head `04fd7e6c6f1700f5979b09d39b6d01d8c7a533b6` failed in run
`35877873704`, job `107238397793`.

The exact failed assertion remained:

```text
Error: layout invariant failed for setup: {"css_viewport":{"width":1280,"height":720},"sidebar":{"left":0,"right":250,"top":-2914,"bottom":720.515625,"width":250,"height":3634.515625},"main":{"left":250,"right":1265,"top":-2914,"bottom":720.515625,"width":1015,"height":3634.515625},"max_sidebar_descendant_right":250,"sidebar_contained":true,"main_begins_after_sidebar":true,"no_horizontal_scroll":true,"clipped_controls":1,"overlapping_cards":[],"active_breakpoint":"wide"}
```

The failure again occurred at `setup 1280x720`, after all earlier packaged
states passed. The prior navigation-level reset did not survive final route
layout settlement. This remains a qualification-harness state-isolation
finding; product failure is not established. The next repair moves the reset
inside the layout invariant, resets the document scrolling element and app
shell roots, and keeps strict clipping and overflow assertions unchanged.

The invariant-level reset fixed the leaked scroll position in the next run.
The remaining aggregate count was one clipped control, but the existing
artifact did not identify that control. The next qualification pass adds its
exact tag, text, and rectangle to failure telemetry before classification.

## Follow-up exact-head failure: clipped YouTube action

The telemetry repair ran in `35884011790`, job `107259399755`, at head
`5402fabe9f223b82bfb4facf0c92020fed259f17`.

The exact failed assertion was:

```text
Error: layout invariant failed for setup: {"css_viewport":{"width":1280,"height":720},"sidebar":{"left":0,"right":250,"top":0,"bottom":3634.515625,"width":250,"height":3634.515625},"main":{"left":250,"right":1265,"top":0,"bottom":3634.515625,"width":1015,"height":3634.515625},"max_sidebar_descendant_right":250,"sidebar_contained":true,"main_begins_after_sidebar":true,"no_horizontal_scroll":true,"clipped_controls":1,"clipped_control_details":[{"left":1103.78125,"right":1282.25,"top":1031.84375,"bottom":1074.5625,"width":178.46875,"height":42.71875,"tag":"BUTTON","text":"Test YouTube support"}],"overlappingCards":[],"active_breakpoint":"wide"}
```

Expected UI state: every visible control remains within the 1280 CSS-pixel
viewport and the Setup Overview remains horizontally contained.

Actual UI state: the visible `Test YouTube support` button extended to
`right=1282.25`, beyond the `1280` viewport and the `1265` main-pane edge.
The sidebar and page roots were at `top=0`; scroll leakage was no longer
present. Horizontal document overflow remained absent, so the failure was a
control-level containment defect.

Screenshot filename: `setup-1280x720.png` in the `clipgauge-windows-setup`
artifact from run `35884011790`.

Classification: product layout defect. The v0.6.2 component action row did
not wrap or bound the YouTube action group at the packaged wide viewport.
This is not a stale selector, fixture, or scroll-reset failure.

Repair: the component action row now wraps, constrains its action group to
the card width, and permits the button label to wrap. The existing strict
clipped-control invariant remains unchanged. A fresh exact-head packaged
qualification is required before this finding can close.

## Post-merge failure: native confirmation text race

The merged main commit `6942c1b74f2707e1de0d6be22f1e4590c6aee9b6` has the
same Git tree as the qualified PR head `4095848543c54a8ab5047ee254994a75f421e7b5`.

The post-merge Windows run `35959114896`, job `107503660125`, failed at
`credential-removal-confirmation 1366x768` in the packaged UI qualification.
Artifact `10791729453` contains the owner screenshot and display evidence,
but not `credential-removal-confirmation-dialog-1366x768.png` or the native
dialog semantic JSON. No 1920x1080, 1920x1200, Create, Sessions, or additional
viewport evidence was produced.

Exact assertion:

```text
Error: native confirmation ContentText missed the non-revocation phrase
```

The native `ClipGauge` dialog was observed with class `#32770`, and its OK and
Cancel controls were exposed. The inspection output showed the expected
provider prefix, but the machine-readable `ContentText` read did not expose
`does not revoke the provider key` during that read. The product source still
passes the complete confirmation message to the native dialog. The same tree
passed this exact semantic check in pre-merge run `35890417348`; its evidence
reported `content_text_nonempty: true`, provider observed, and the full phrase
observed. This identifies a harness timing race, not a confirmed product
contract defect.

Debug rerun attempt 2 retained SHA
`6942c1b74f2707e1de0d6be22f1e4590c6aee9b6` and failed with the same assertion
in job `107528559553`. Its new artifact is `10795860061`. The minimal harness
repair adds a bounded retry for incomplete native `ContentText`; provider and
non-revocation assertions remain strict. A fresh exact-head Windows run is
required before release authorization.

## PR #58 repair validation

PR #58 head `f8b1a30e508db2749c3c4219a0546ff9d1a40769` ran the bounded retry
on Windows workflow `35971603954`, job `107542368914`. The packaged UI step
still failed at `credential-removal-confirmation 1366x768` with the same exact
assertion. Artifact `10797226639` again contains the owner screenshot and
display evidence, but no native dialog screenshot or semantic dialog JSON.

The retry made six-second repeated `winapp ui get-value ContentText` reads.
The native window remained `#32770`, titled `ClipGauge`, owned by the app PID,
with OK and Cancel controls. The machine-readable WinAppCLI value remained
incomplete. This rules out a simple delayed-read-only explanation for this
runner path. It does not establish a product defect: the same source tree
passed the strict full-message check in pre-merge workflow `35890417348`, and
the product source still supplies the complete confirmation sentence.

The remaining classification is harness/tool-path. The next focused repair
adds a direct Windows UI Automation TextPattern read as a fallback. It still
requires the provider name and exact non-revocation phrase. It does not accept
the elided `inspect` text, screenshots, or known source text as substitutes.
