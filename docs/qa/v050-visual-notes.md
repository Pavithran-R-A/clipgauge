# v0.5.0 visual QA notes

The current v0.5.0 frontend was inspected at 895×768 through the real running application. The first-run hero uses the deep-ocean canvas, teal accents, oversized creator-facing heading, and a clear setup action. The next onboarding screen exposes local setup approval, a remaining-component count, a single install action, and a Continue path. The final onboarding screen explains local storage and the local-versus-cloud boundary before opening Create.

The browser inspection confirms the visible copy is creator-facing and that the onboarding path has a clear next action. The packaged Linux artifact was built separately as `app/src-tauri/target/release/bundle/deb/ClipGauge_0.5.0_amd64.deb`; a native desktop screenshot still needs to be captured from the packaged window before README screenshot links are considered complete.
At 895×768, Create shows persistent navigation with Create, Sessions, Setup & Storage, AI Providers, Integrations, Privacy, Help & Diagnostics, and About. The content follows three numbered steps: Add a video, Choose AI, and Choose caption style. The right rail explains what happens next and summarizes the selected AI, captions, and vertical output. The screen has clear local-first and Privacy/Setup escape actions. The lower caption-style section continues below the viewport, so responsive QA must include the 1280×720 and 2560×1440 desktop sizes plus a mobile-width check.
The packaged `clipgauge-app` binary was launched on the active display and captured at 1200×800. The native Create screen has no browser chrome or annotation overlay, shows the persistent creator navigation, the Add a video card, the What happens next rail, and the Your choices summary. It is suitable as the README's primary real screenshot. The native first-run hero and privacy-summary screens were also captured at 1200×800. The Review screen still needs a genuine result fixture or a deterministic way to open a completed session before a public Review screenshot is added; no mockup will be substituted.
A deterministic headless matrix captured Create at 1280×720, 1440×900, 1920×1080, and 2560×1440. The 1280×720 view keeps the navigation, add-video form, and right rail inside the viewport with page scrolling for lower steps. The 2560×1440 view remains centered with a balanced form-and-rail grid and no stretched full-width content. These captures are QA evidence only; the README uses the separate native packaged screenshots.
The live Create-screen accessibility sweep found zero unnamed buttons, zero unlabeled form controls, zero images without alt text, and a logical H1→H2 heading sequence. The live AI Providers screen exposes all ten supported providers, keeps custom configuration under Advanced settings, and shows local privacy facts for ClipGauge Local. The screen was inspected at 895×768 with the provider cards remaining keyboard-reachable semantic buttons.
The live Setup & Storage screen shows the four grouped headings: Video tools, Speech recognition, Speaker & audio analysis, and YouTube support, plus Optional local AI. It exposes one Install required components action and uses “Size calculated during setup” rather than a zero-byte placeholder. The lightweight scanner found zero unnamed buttons and no literal `0 B`; its one unlabeled-input count is a false positive because the approval checkbox is correctly labeled by a wrapping `<label>` element, which the simple scanner did not inspect.
After the explicit label association, the Setup & Storage sweep found zero unnamed buttons, zero unlabeled inputs, no literal `0 B`, and one Install required components action. The live Integrations screen clearly separates Pexels stock visuals from Instagram performance feedback and explicitly says the screen is not beside AI provider settings.
The packaged native Privacy screen loaded its Rust-backed summary successfully. It presents Local-first mode, Stays on this computer, Sent to your AI provider, and Other network activity as separate sections, with technical details available below the main explanation. This confirms the browser-only invoke error is a development-runtime limitation, not the packaged path.
After rebuilding with `CARGO_BUILD_JOBS=1`, the packaged `clipgauge-app` binary was recaptured at 1200×800. The final native Create screenshot shows the intended workspace, not onboarding, with the current deep-ocean shell and creator-first three-step flow. The earlier blank capture was caused by the browser covering the native window; the final screenshot was taken after raising the native window and visually verified.
The final rebuilt package also produced a 1200×800 native Sessions screenshot, matching the Create capture's binary and visual system. The README now points only to these two verified packaged screenshots.
