# ClipGauge v0.5.17 Real-Device Stabilization

Date: 2026-09-13

Candidate status: local stabilization branch only.

No v0.5.17 merge, tag, or release exists.

## Regression status

| Area | Result | Evidence |
|---|---|---|
| R01 managed-WAV CPU ASR | PASS in automated coverage | ClipGauge-owned RIFF/WAVE loader; typed format, truncation, size, and clean-PATH tests. |
| R02 YouTube repair | PASS in automated coverage | Atomic node/source/plugin repair; complete-build postcondition; typed install failures; idempotent repair fixture. |
| R03 result contract | PASS | Python adjustment reason; Rust artifact preservation; strict TypeScript validation; historical normalization; preserved v0.5.16 session loaded successfully. |
| R04 elapsed lifecycle | PASS | Success, failure, cancellation, terminal, exit, and resume-error paths freeze elapsed time. |
| R05 runtime notice | PASS | Runtime warning clears on job or progress events. |
| R06 AI modes | PASS | Local mode names ClipGauge Local; cloud modes require configured provider state; provider/model summary is shown. |
| R07 regression matrix | PASS | Windows CPU fixtures, production CPU ASR, provider lifecycle and repair, public YouTube transfer, and preserved-result contract tests. |
| R08 incident records | RECORDED | Three incident classes are listed below without private paths. |
| R09 security boundaries | PASS | Gitleaks clean; npm production audit clean; approved VEX and Cargo warning records remain visible; loopback binding and no-cookie boundaries verified. |
| R10 release surfaces | COMPLETE | All authoritative versions now report 0.5.17. |

## Incident records

- Owner laptop: successful processing followed by Review-loading regression.
- Mom laptop: `ASR_AUDIO_LOAD_FAILED`, CPU/int8/low-memory/batch1.
- GF laptop: `YOUTUBE_BUILD_REQUIRED`, provider repair did not heal the install.

## Provider provenance

The managed bgutil provider uses upstream tag `2.0.0`.

Source archive SHA-256:

`e95324ee24b1b0f1b4ad43d336343afe7cf1914acdf65d9cc1977f51d7b137c2`

The plugin is copied only from that archive.

The server binds to `127.0.0.1` and `::1`.

Browser cookies remain opt-in only.

## Qualification limits

The physical i5-8265U / 8 GB laptop run remains deferred.

This deferred run is post-release testing.

It is not a release blocker.

The current owner Windows qualification host completed CPU and public-link runs.

The physical i5 run is still post-release testing.

## Automated evidence

- Python: `649 passed`, full suite.
- Frontend: `248 passed`, full suite.
- Rust: `97 passed`, full suite.
- Rust clippy: passed with warnings denied.
- Cargo audit: seven owner-approved advisory warnings remain visible.
- Frontend production build: passed.
- v0.5.16 immutability: preserved.

## Owner-host qualification evidence

- Production CPU path: PASS. Real `IngestStage` to `AsrStage` completed with
  CPU/int8/low-memory/batch-1 selection, 22 words, exact alignment, and an
  11.0-second audio artifact. System FFmpeg was absent from PATH; managed
  FFmpeg supplied the media path.
- Public YouTube transfer: PASS. Video ID `jNQXAC9IVRw` returned metadata,
  media transfer, probe, and extracted `audio16k.wav` successfully. The run
  used bgutil `2.0.0`, IPv4 loopback, no cookies, no observed HTTP 403, and
  no attestation retry. Transferred media size was `475989` bytes.
- Preserved v0.5.16 result: PASS. Session
  `20260913-184628-1c4cf6` loaded through the current result contract with
  five score clips, five render outputs, and all five render artifacts
  present. Historical additive bonuses and numeric story scores were accepted
  without changing the stored values.
- Provider boundary: PASS. The live service listened only on `127.0.0.1`
  and `::1`; browser-originated cross-site requests returned `403`.
