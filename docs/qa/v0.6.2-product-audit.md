# ClipGauge v0.6.2 Product Audit

Date: 2026-09-23
Branch: `fix/v062-product-quality`
Base: `de45e95f68f71ad4c37b1720e2c257fe1953b28c`
Scope: product-quality repair only.

## Audit boundary

This audit covers confirmed v0.6.1 defects. It excludes new v0.7 features. The immutable `v0.6.1` tag remains outside this branch.

The audit separates these evidence classes:

- confirmed local evidence;
- automated regression evidence;
- remote qualification evidence;
- real-device evidence.

No local test substitutes for real-device qualification.

## YouTube evidence

The source diagnostic was:

`C:\Users\Pavithran R A\.clipgauge\jobs\20260922-184315-5677f5\diagnostics\diag-cc677382bf424c3d.json`

Observed facts:

- source platform was YouTube;
- failure phase was `GVS_TRANSFER`;
- method was `mweb`;
- provider was `bgutil-http`;
- partial file size was `118,962,706` bytes;
- transfer ran for about `417.579` seconds;
- exact HTTP status was unavailable;
- exact upstream reason was unavailable.

The old implementation retained only stderr. It discarded useful stdout. It then classified the failure generically. Recovery retried `mweb` only for a narrow attestation and HTTP-403 combination.

This proves a transfer failure under the recorded method. It does not prove a single upstream cause.

Fresh qualification evidence:

- The repaired app self-test passed on 2026-09-23.
- Provider version was `2.0.0`.
- Runtime was yt-dlp `2026.08.19`.
- A known public sample downloaded successfully.
- Sample bytes were `155,343,423`.
- Sample SHA-256 was `2fa151767bdc727368530f587ac74b139404514a0690e4c0d7eb2b6ac2bf19da`.
- Sample probing found valid MP4 media.
- The original source timed out after bounded 300-second testing.
- That original source remains unqualified.
- No blind retry was launched.

## Confirmed findings and repairs

| Finding | Repair | Regression evidence |
| --- | --- | --- |
| YouTube diagnostics lost stdout | Bounded redacted stdout and stderr capture | ingest reliability tests |
| Error messages were unstable | Stable ingest error codes | ingest reliability tests |
| Recovery retried one method | Documented `mweb` to `web_safari` fallback | ingest reliability tests |
| Public transfer state was conflated | Separate attempt and last-success fields | readiness tests |
| Failed sessions lacked identity | Persisted source labels and terminal summaries | Rust and frontend tests |
| Terminal state was inferred | Added redacted atomic `terminal.json` | protocol tests |
| Diagnostic IDs appeared as primary copy | Added technical-details disclosure | error presentation tests |
| Creation controls were flat | Added visible intent and quick mode | Studio tests |
| Internal stages overwhelmed users | Collapsed stages into three phases | progress tests |
| Setup exposed every control | Added Overview, Storage, and Advanced tabs | frontend build and tests |

The ASR resource guard remains unchanged. No cookie import was added. No credential is stored in the profile.

## Confirmed finding disposition

All confirmed v0.6.2 product-quality findings are `CLOSED`.

| Confirmed finding | Status | Evidence |
| --- | --- | --- |
| Packaged Create qualification asserted removed copy | `CLOSED` | Semantic v0.6.2 contract; stale Step 2/3 assertions removed |
| Sessions lacked deterministic mixed-state coverage | `CLOSED` | Five isolated supported job fixtures; mixed-state screenshot and JSON |
| Failed YouTube sessions lacked actionable identity | `CLOSED` | `Needs attention`; `Download failed`; safe removal action |
| Sidebar and Sessions could diverge | `CLOSED` | Shared `sessionPresentation` mapping; sidebar comparison evidence |
| Setup readiness summary was incomplete | `CLOSED` | Overview labels AI, acceleration, YouTube, and storage |
| Setup technical details were too prominent | `CLOSED` | Storage and Advanced separation; technical disclosure |
| Create hierarchy was ambiguous | `CLOSED` | Source, intent, scoring, output, collapsed Advanced, Create action |
| Packaged Create qualification required an exact radio accessible name despite descriptive text | `CLOSED` | Run `35852523884`; semantic radio role plus visible-label matching |
| YouTube transfer failure could hang or misclassify | `CLOSED` | Bounded capture, stable error, fallback, readiness separation |
| Packaged qualification used an unbootstrapped isolated home | `CLOSED` | Run `35847813904` artifact `10744579444`; clean-home handoff repair and regression test |
| Packaged qualification expected no-recommendation copy in the wrong field | `CLOSED` | Run `35861975944`; current state/detail/action contract and regression test |
| Packaged qualification leaked scroll between viewport states | `CLOSED` | Run `35867006588`; per-viewport scroll reset and strict layout invariant regression test |

## Windows visual evidence

The real installed Windows package was inspected at native 125% scaling.

- Applied DPI: `120` from `GetDpiForWindow`.
- Device pixel ratio: `1.25`.
- Logical viewport: `1200x800`.
- Horizontal overflow: absent; `scrollWidth <= clientWidth`.
- Create normal: `create-normal-125.png`.
- Create Advanced: `create-advanced-125.png`.
- Running progress: `running-progress-125.png`.
- Sessions mixed state: `sessions-mixed-cdp.png`.
- Setup Overview: `setup-overview-125.png`.
- Storage: `setup-storage-125.png`.
- Advanced Setup: `setup-advanced-125.png`.
- Review: `review-125.png`.
- Collection dialog: `collection-125.png`.

The installed package displayed the five mixed session fixtures:
Ready, failed YouTube, Cancelled, no recommendations, and Continue.
The failed row exposed `Remove failed session` safely.
The ready fixture opened Review and Collections.

The isolated home lacked downloaded core components.
Therefore setup showed `Core setup needed` honestly.
This is an environment state, not a UI defect.

## Security evidence

- npm audit: `0` vulnerabilities.
- Cargo audit: `0` vulnerable advisories; seven known VEX warnings.
- Python locked audit: `160` resolved packages exported from `uv.lock`.
- Hash-aware installation was blocked by a ctranslate2 hash mismatch.
- No-hash, no-dependency audit completed against that locked set.
- Nine raw records appeared in NLTK and Transformers.
- CUDA Torch variants were skipped as non-PyPI distributions.
- Python VEX reconciliation: all nine records match existing dispositions.
- Python new findings: `0`.
- Audit tool failures were root-caused, not left inconclusive.
- Gitleaks: staged scan remains required after final commit.

The current audit is conclusive.
No dependency was changed to obtain it.

## Remaining qualification gates

Fresh evidence remains required for:

- model-backed E2E on final PR head;
- exact-head remote CI, Docker, Windows, and macOS;
- known public YouTube sample on final PR head;
- final review status and mergeability.

The original YouTube source remains case-specific.
It is environmentally/upstream unqualified, not global failure.
The bounded failure and fallback are confirmed.

This document does not authorize release.

## Verification commands

The focused checks already passed locally:

- Python ingest reliability tests;
- setup and stability regression tests;
- protocol tests;
- Rust unit tests;
- focused frontend tests.

The full local suite passed after the harness repair.
The exact-head remote Windows gate remains required.
